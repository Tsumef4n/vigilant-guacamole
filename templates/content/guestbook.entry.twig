<div>
  <p class="guestbook-header">
    <b>{{ entry.name }}</b>
    {% if auth.check %}
      ({{ entry.email }})
    {% endif %}
    schrieb am
    {{ entry.created_at|date("d.m.Y") }}
    um
    {{ entry.created_at|date("H:i") }}:</p>
  <p>{{ entry.text }}</p>
  {% if auth.check %}
    <p>
      <form action="{{ path_for('admin.guestbook.delete', { 'id': entry.id}) }}" method="POST" onsubmit="return confirm('Soll der Eintrag gelöscht werden?');">
        <button type="submit" class="btn btn-danger btn-xs">Delete</button>
        {{ csrf.field | raw }}
      </form>
    </p>
  {% endif %}
</div>
